# Memoria 未来扩展方向

这份路线图记录 Memoria 后续值得考虑的功能方向。优先级倾向于先增强现有纯 Markdown 工作流，再考虑更重的智能化能力。

## 推荐优先级

## 已落地功能

### 2026-06-03

- 回收站 UI：新增独立回收站视图，支持搜索、恢复、永久删除和清空回收站。
- 保存筛选器：侧栏可保存当前筛选条件，并一键复用或删除。
- memo 转正式笔记：卡片菜单可创建独立 Markdown 笔记，保留来源时间和原始文件路径。
- 标签整理工具：新增独立标签整理视图，支持标签搜索、重命名/合并、移除标签。

### 1. 未整理收件箱与转正式笔记

Memoria 目前很适合捕捉碎片，但还可以补一个“把碎片长成笔记”的出口。

可扩展能力：

- 一键将 memo 转成独立 Markdown 笔记。
- 将 memo 追加到当前打开文件。
- 支持生成双链引用。
- 支持套用模板，例如日记、实验记录、灵感卡片。
- 转出后可选择保留原 memo、添加已整理标签，或移动到归档区。

价值：

- 让 Memoria 不只是收集箱，也能成为正式笔记系统的入口。
- 和 Obsidian 的双链、模板、文件组织天然衔接。

当前状态：

- 已支持创建独立 Markdown 正式笔记。
- 后续可继续补追加到当前文件、模板选择、整理状态标签等能力。

### 2. 更强的回顾系统

当前已有每日回顾和随机回顾，后续可以发展成更主动的间隔回顾。

可扩展能力：

- 很久没看过的 memo 自动浮现。
- 同标签、同情绪、同主题的旧 memo 加权推荐。
- 往年今日、近一周、近一月多层级回顾。
- 未完成待办自动进入回顾队列。
- 为已回顾 memo 记录最近回顾时间，避免重复刷到。

价值：

- 让旧碎片重新参与当前思考。
- 提高长期记录的复利，而不是只做时间线堆积。

### 3. 回收站 UI

当前 `_trash.md` 已能保证误删可恢复，但恢复过程仍偏手动。

可扩展能力：

- 新增回收站视图。
- 查看、搜索、筛选已删除 memo。
- 一键恢复到原日期、原年份文件。
- 支持永久删除单条或清空回收站。
- 展示来源文件和原始记录时间。

价值：

- 提升删除操作的安全感。
- 让 `_trash.md` 从底层兜底变成完整功能。

当前状态：

- 已支持独立回收站视图、搜索、恢复、永久删除和清空。
- 后续可继续补批量恢复、按日期分组、恢复前预览目标位置。

### 4. 保存筛选器

高级搜索已有基础语法，可以进一步把常用查询沉淀为侧栏入口。

可扩展能力：

- 保存当前筛选条件。
- 为筛选器设置名称和图标。
- 在侧栏固定展示常用筛选器。
- 支持编辑、排序、删除筛选器。

示例：

- 实验记录：`#实验`
- 近一个月待办：`after:2026-05 -#完成`
- 有图灵感：`#灵感` + 有图片筛选

价值：

- 降低重复输入搜索语法的成本。
- 让高级搜索真正变成可长期使用的工作流。

当前状态：

- 已支持保存当前筛选、侧栏复用和删除。
- 后续可继续补排序、改名、图标选择和导入导出筛选器。

### 5. 导入器

Memoria 的纯 Markdown 存储适合作为碎片笔记迁移终点。

可扩展能力：

- 从 Blinko 导入。
- 从 flomo 导入。
- 从 CSV / JSON 导入。
- 从普通 Markdown 批量导入。
- 支持导入前预览、字段映射、重复检测。

价值：

- 降低迁移成本。
- 让 Memoria 更容易接住已有历史数据。

### 6. 标签整理工具

长期使用后，标签会逐渐膨胀，需要维护能力。

可扩展能力：

- 标签重命名。
- 标签合并。
- 查找孤立标签、低频标签。
- 按标签批量添加、移除或替换。
- 检查保留标签 `#置顶`、`#收藏` 的异常使用。

价值：

- 避免标签系统长期失控。
- 保持回顾和筛选的质量。

当前状态：

- 已支持标签搜索、按影响 memo 数排序、重命名/合并标签、移除标签及其子标签。
- 后续可继续补批量选择多个标签、标签改名预览、低频标签一键清理。

### 7. 周报与月报生成

基于现有数据报告，可以生成更面向输出的 Markdown 总结。

可扩展能力：

- 生成本周 / 本月记录摘要。
- 汇总 Top 标签、活跃天数、记录数量。
- 收集未完成待办。
- 抽取值得回看的 memo。
- 输出到指定 Markdown 文件。

价值：

- 把碎片记录转成周期性复盘材料。
- 适合学习、项目推进、实验记录和生活复盘。

### 8. 补齐已有设置入口

代码中已经有部分设置入口，例如 Vim 快捷键、智能回顾，需要确认并补齐实际行为。

可扩展能力：

- 完整实现 Vim 快捷键：`j/k` 选择卡片、`Enter` 编辑、`/` 搜索、`i` 新建、`gg/G` 跳转。
- 完整实现智能回顾：基于时间、标签、情绪、最近回顾状态进行加权抽样。
- 对未完成或实验性设置给出清晰状态，避免用户误解。

价值：

- 提高设置页与实际行为的一致性。
- 优先消化已有架构中的“半成品”能力。

### 9. 可选 AI 功能

AI 能力适合放在后期，并且应保持可选。

可扩展能力：

- 自动标签。
- 语义搜索。
- 相似 memo 聚类。
- 周报 / 月报自动摘要。
- 从碎片生成正式笔记草稿。

设计原则：

- 默认关闭。
- 不破坏纯 Markdown 数据主权。
- 明确区分本地处理和外部 API。
- 允许用户检查、修改、拒绝 AI 生成结果。

价值：

- 提升整理和回顾效率。
- 但不应取代 Memoria 的轻量、本地、可控核心。

## 建议近期路线

近期已完成：

1. 回收站 UI。
2. 保存筛选器。
3. memo 转正式笔记。

理由：

- 都贴近当前架构。
- 不需要引入重依赖。
- 对真实使用体验提升明显。
- 能继续强化 Memoria 的纯 Markdown 工作流。
